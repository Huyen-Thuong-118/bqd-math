// Admin: form tạo đề mới — upload 2 file (đề + đáp án), cấu hình thời gian
// giao đề, cho phép download hay không, sinh phiếu tô đáp án theo số câu.
export default function CreateExamPage() {
  return (
    <section className="space-y-4">
      <h1 className="text-xl font-semibold text-navy-600">Tạo đề mới</h1>
      {/* TODO: <FileUpload /> x2 + form cấu hình (features/exams) */}
    </section>
  );
}

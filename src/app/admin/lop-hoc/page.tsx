// Admin: quản lý nhiều lớp (tạo lớp, thêm/xoá HS, upload tài liệu vào lớp).
export default function AdminClassesPage() {
  return (
    <section className="space-y-4">
      <h1 className="text-xl font-semibold text-navy-600">Quản lý lớp học</h1>
      {/* TODO: bảng danh sách lớp + nút "Tạo lớp mới" */}
    </section>
  );
}

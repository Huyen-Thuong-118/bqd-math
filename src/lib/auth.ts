// Cấu hình NextAuth (Auth.js) — session, providers, callbacks phân quyền.
// TODO: implement khi cài next-auth. Xem README.md phần "Bước tiếp theo".
//
// export const { handlers, auth, signIn, signOut } = NextAuth({ ... })
